// Generated by CoffeeScript 1.10.0
(function() {
  var WebSocketTest, messageContainer,
    indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

  messageContainer = document.getElementById("messages");

  WebSocketTest = function() {
    var ws;
    if ((indexOf.call(window, "WebSocket") >= 0)) {
      messageContainer.innerHTML = "WebSocket is supported by your Browser!";
      ws = new WebSocket("ws://localhost:8585/sessions/?Id=123456789");
      ws.onopen = function() {
        return ws.send("Message to send");
      };
      ws.onmessage = function(evt) {
        var received_msg;
        received_msg = evt.data;
        return messageContainer.innerHTML = "Message is received..." + received_msg;
      };
      return ws.onclose = function() {
        return messageContainer.innerHTML = "Connection is closed...";
      };
    } else {
      return messageContainer.innerHTML = "WebSocket NOT supported by your Browser!";
    }
  };

}).call(this);

//# sourceMappingURL=graph.js.map
