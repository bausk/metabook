// Generated by CoffeeScript 1.10.0
metabook.data = {};

metabook.data.get_template = function(success, error) {
  return $.ajax({
    url: metabook.uri.template_endpoint + metabook.uri.file.path,
    type: 'GET',
    success: success,
    error: error
  });
};

metabook.data.get_file = function(success, error) {
  return $.ajax({
    url: metabook.uri.file.endpoint + metabook.uri.file.path,
    type: 'GET',
    success: success,
    error: error
  });
};

metabook.data.get_ajax_data = function(data_uri, success, error) {
  return $.ajax({
    url: data_uri,
    type: 'GET',
    success: success,
    error: error
  });
};

metabook.data.get_xhr = function(data_uri) {
  return $.ajax({
    url: data_uri,
    type: 'GET'
  });
};

metabook.data.is_good_form = function(json_data) {
  if (typeof json_data === 'object') {
    if ('metadata' in json_data) {
      if ('metabook' in json_data.metadata) {
        if ('links' in json_data.metadata.metabook) {
          return true;
        }
      }
    }
  }
  return false;
};

metabook.data.is_native = function(file_json) {
  if ('metadata' in file_json) {
    if (file_json.metadata.format === "native") {
      return true;
    }
  }
  return false;
};

metabook.data.get_cells = function(metabook_model) {
  return metabook_model.get('cells').models;
};

metabook.data.get_links = function(metabook_model) {
  return metabook_model.get('links').models;
};

//# sourceMappingURL=data.js.map
